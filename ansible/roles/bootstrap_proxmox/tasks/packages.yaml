---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install common Proxmox packages
  ansible.builtin.apt:
    name: "{{ proxmox_common_packages }}"
    state: present
